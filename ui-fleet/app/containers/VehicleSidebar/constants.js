/*
 *
 * VehicleSidebar constants
 *
 */

// import { createAsyncTypes } from 'utils/actionUtils';
const PREFIX = 'Vehicle_SIDEBAR_';
export const actionTypes = {
  CHANGE_TAB_INDEX: `${PREFIX}CHANGE_TAB_INDEX`,
  RESET_TAB_INDEX: `${PREFIX}RESET_TAB_INDEX`,
};
