// import { fromJS } from 'immutable';
// import { selectVehicleInfoManagementDomain } from '../selectors';

describe('selectVehicleInfoManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
